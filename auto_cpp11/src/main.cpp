#include <iostream>

/// auto
/// - 在编译期间自动推导出变量的类型
/// - 使用 auto 类型推导的变量必须马上初始化
/// - 一个auto对应一个类型，相当于占位符
/// - 可以和某些具体类型混合使用，根据上一条进行理解
///
/// 限制
/// - auto 不能在函数的参数中使用
/// - auto 不能作用于类的非静态成员变量（也就是没有 static 关键字修饰的成员变量）
/// - auto 关键字不能定义数组，比如下面的例子就是错误的
///
/// 使用场景
/// - 使用 auto 定义迭代器
/// - auto 用于泛型编程 编写模板类时经常碰到不知道具体类型的情况
///





int main(int argc, char *argv[])
{


  return 0;
}

